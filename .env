# Active configuration for this instance of the RTSP camera backend.
# See .env.example for the full explanation of every variable.

RTSP_CONFIG=config.yaml
RTSP_HOST=0.0.0.0
RTSP_PORT=8090
RTSP_ACTIVE_CAMERA=front_door
RTSP_STATS_INTERVAL=5.0
